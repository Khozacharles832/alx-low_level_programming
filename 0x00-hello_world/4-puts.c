#include <stdio.h>
/**
 * Main - A program that prints a line with the put function
 * Return: 0 (a return of 0, means a successful program)
 */
int main (void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
