#include "main.h"
#include <stdio.h>

/**
 * main - prints the name of the program followed by \n
 * @argc: - argument count
 * @argv: - argument vector, an array that stores the arguments
 *
 * Return: 0
 */
int main(int argc, char **argv)
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
